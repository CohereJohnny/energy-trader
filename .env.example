# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=energy_trader
DB_USER=postgres
DB_PASSWORD=postgres

# Docker Compose will use these for the PostgreSQL container
# If running locally without Docker, update DB_HOST accordingly

